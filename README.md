# fleet-manager-testbed

Test target repository for [fleet-manager](https://github.com/cerbu-tech/fleet-manager)
integration checks: worker sessions clone this repo, make small changes, and publish
artifacts back as branches + PRs. `main` is branch-protected on purpose — direct
pushes must be rejected.
